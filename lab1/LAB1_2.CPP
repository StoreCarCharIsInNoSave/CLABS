/* Программирование
лабораторная работа 1 основные понятия языка с
форматный ввод и вывод даннх. Циклы  и ветви
Вариант 10 Индивидуальное задание
 Посчитать значение функции в завсисимости от х
    sin x + cos x,                 x<-2,
  (x^2 + 24)/((1+x)^2),         -2<=x<2,
    cos x + (1/sqrt(x-4)),          x>2
    вычисление функции реализовать с помощью операции условие(?:)
    использовать функции форматного вывода
*/


#include <stdio.h>     // standard  input ? output  header
#include <math.h>
#include <conio.h>    // console  input ? output  header

int main()
{

  float x, f, z; 
  int t = 1;
  clrscr();

  printf ("\n   Вычислить значение функции в зависимости от значения аргумента ");
  printf ("\n                       ");
  printf ("\n    sin x + cos x,                 x<-2,");
  printf ("\n  (x^2 + 24)/((1+x)^2),         -2<=x<2,");
  printf ("\n     cos x + (1/sqrt(x-4)),          x>2.");
  printf ("\n                      ");
  printf ("\nВычислить значение функции в зависимости от значения аргумента");
  printf ("\n использовать функцию форматного вывода");
  printf("\n введите значение х->");
  scanf ("%f", &x);


  f = x < -2 ? (sin(x)+cos(x)) : (x >=-2 && x<2 ? (x!=-1 ? (x*x+24)/((1+x)*(1+x)) : (t = 0)) : (x>4 ? (cos(x) + 1/sqrt(x-4)) : (t=0) ));


  z = x == -1 ? (2) : (3);

  clrscr();


  printf ("\n    Вычислить значение функции в зависимости от значения аргумента ");
  printf ("\n                      ");
  printf("\n    sin x + cos x,                 x<-2,");
  printf("\n  (x^2 + 24)/((1+x)^2),         -2<=x<2,");
  printf("\n     cos x + (1/sqrt(x-4)),          x>2.");
  printf ("\n                      ");
  printf ("\n  Вычислить значение функции в зависимости от значения аргумента");
  printf ("\n  использовать функцию форматного вывода");


  printf ("\n  Исходные данные \n");
  printf ("Аргумент х = %-6.2f",x);


  t == 1 ? printf("\nПри х=%-6.2f функция у =%-10.3f определена по ветви %-6.0f", x, f, z) :
  printf("\n При х=%-6.2f функция не определена(деление на нуль)по ветви  %-6.0f", x, z);
  getch();

  return 0;
}


