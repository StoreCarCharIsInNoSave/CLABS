/*Методы программирования и информатика
  Лабораторная работа №2. Основные Использование циклов.
  Работа с целыми числами

  Типовое задание 1.
  Условие: Из введённого числа получить новое число путём уменьшения
  каждой цыфры на еденицу.

  Выполнил студент группы PM-22 Orlovskiy A.C.
*/

// подключение заголовочных файлов (директивы препроцессора)

#include <stdio.h>
#include <conio.h>
//Для десятичной системы счисления
int main()
{
  unsigned long a, a1, b;      // a - исходное число, b - результат
  long int c, k = 1;            // c - текущая цифра числа а, k - разряд
  // вывод условия задачи
  clrscr();
  printf ("\n Условие: Из введённого числа получить новое число путём уменьшения");
  printf ("\n каждой цыфры на еденицу.");
  printf ("\n Введите число: ");
  scanf ("%lu",&a);
  a1=a;
  b = 0;	// начальное значение числа b
  // пока a != 0 выделяем очередную цифру и обрабатываем ее
  while (a)
   {
    c = a % 10;      // выделяем последнюю цифру
    if (c != 0)
     {
      c--;
      b += c * k;  //добавление к новому числу цифру
     }
    k *= 10; // увеличиваем разряд
    a /= 10;   // уменьшаем число а (отбрасываем последнюю обработанную цифру)
   }
  clrscr();
  printf ("\n Условие: Из введённого числа получить новое число путём уменьшения");
  printf ("\n каждой цифры на единицу.");
  printf ("\n Введённое число: %lu",a1);
  printf ("\n Преобразованное число= %lu",b);
  getch ();
  return 0;
}

